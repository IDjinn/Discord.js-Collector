const CollectorValidator = require('../../structures/collectorValidator');

module.exports = class ReactionCollectorValidator extends CollectorValidator {
    constructor(options) {
        super(options);
    }
}